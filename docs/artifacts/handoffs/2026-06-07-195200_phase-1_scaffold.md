# Turnover: Phase 1 — Scaffold & Theme System (mid-phase)

- Created: 2026-06-07 19:52:00
- Project: C:/Users/jkhbu/Desktop/codex/nd-portfolio
- Branch: unknown — git could not be run this session (terminal backend down); no phase branch/commit created yet
- Continues from: none (first handoff)

> **Implementer: start here.** The Phase 1 code is fully written but **never executed** — the terminal backend was down all session, so `npm install` / `next build` / `next dev` have not run. Your first job is to install, run, and fix whatever the build surfaces, then finish verifying Phase 1's acceptance criteria. Then continue per `docs/01_scaffold-theme.md` and the phased-workflow Phase Execute mode. Do **not** start Phase 2 until Phase 1's "Tests that must pass" are green.

## 1. Current phase number and status
Phase 1: Scaffold & Theme System — **in progress (blocked on verification, not on design)**. All scaffold + theme-shell files are authored; they need a real install + build pass and the four-theme/no-flash visual checks.

### Immediate next steps (in order)
1. `npm install` (generates `node_modules` + lockfile — neither exists yet).
2. `npm run dev`, open http://localhost:3000. Expect: nav (wordmark + 4 links + Dusk/Pond + Light/Dark controls) and footer, with empty anchored section stubs between them.
3. **If the build errors** `Missing weight for font Cormorant_Garamond`: in `src/app/layout.tsx`, add `weight: ["400", "500", "600"]` to the `Cormorant_Garamond({...})` loader, then rerun. (Configured as a variable font; this is the one known risk.)
4. Verify Phase 1 "Tests that must pass" (see `docs/01_scaffold-theme.md`): all 4 palette×mode combos switch + persist across reload; no theme flash on reload; nav `scrolled` past 40px; scroll-spy active link; reveal honors reduced-motion; no hydration warnings; `npm run build` + `npm run lint` clean.
5. Append results to the phase file execution log; if green, proceed to Phase 2 (Hero + About from `profile.ts`).

## 2. What was completed this session, file by file
| File | Change | Why |
|------|--------|-----|
| `package.json` | Next ^16 / React ^19 / TS deps + scripts; `engines.node >=20.9` | Project manifest |
| `tsconfig.json`, `next.config.ts`, `next-env.d.ts`, `eslint.config.mjs`, `.nvmrc` | Standard Next 16 TS + flat-ESLint config; pin Node 24 | Toolchain |
| `.gitignore` | node_modules, `.next/`, `out/`, `.env*`, `.vercel`, `*.tsbuildinfo`, `next-env.d.ts`, OS/editor cruft | Requested ignore setup |
| `src/app/globals.css` | Full design-system port (palette tokens, dark mode, all component styles); **dropped** `wire` palette + font-token block; added unified `.skill-list` styles | Theme backbone (ADR-0004/0007/0003) |
| `src/app/layout.tsx` | `next/font` for 4 families → CSS vars; no-flash inline theme script; grain layer gated by `profile.theme.grain`; `suppressHydrationWarning` on `<html>` | Fonts + theming + no flash (ADR-0006) |
| `src/app/page.tsx` | Composes Nav + anchored section stubs (`#top/#about/#photography/#work/#skills`) + Footer | Shell; sections filled in later phases |
| `src/components/Reveal.tsx` | IntersectionObserver fade/slide-in; reduced-motion aware (div wrapper) | Scroll reveal |
| `src/components/ThemeControls.tsx` | Dusk/Pond + Light/Dark segmented controls; sets `data-*` on `<html>` + localStorage | Theme toggle |
| `src/components/Nav.tsx` | Wordmark + links + scroll-spy + `scrolled` state; embeds ThemeControls | Top bar |
| `src/components/Footer.tsx` | Footer name/blurb/links/copyright from `profile.ts` | Footer |
| `src/content/types.ts` | Types for all content (Photo, Project, Certification, Profile, …) | Editable-content contract |
| `src/content/profile.ts` | Hero/About/intros/footer/theme text from handoff, with owner comments | Bio/description editing surface |
| `src/content/photos.ts` | 5 photos; "append at bottom → newest first" convention + how-to comment | Photography data |
| `src/content/projects.ts` | 2 projects (Tonnelle, PKG 6); same convention + comment | Built Work data |
| `src/content/skills.ts` | Software list + certifications (name/sublabel) | Skills data |
| `public/images/{portrait,photography,work}/.gitkeep` + `public/images/README.md` | Asset folders + placement guide | Where owner drops images |
| `README.md` | Dev setup + content-location pointer (full owner guide is Phase 5) | Repo readme |
| `CLAUDE.md`, `docs/00_plan.md`, `docs/01_scaffold-theme.md` | Plan index + master plan + phase-1 file (execution log updated) | Phased-workflow docs |
| `docs/decisions/0001–0007*.md` | 7 ADRs (see field 4) | Decision log |

## 3. Test status
- Passing: **none run** — terminal backend unavailable all session; `npm install`/`build`/`dev`/`lint` never executed.
- Failing: none observed (nothing was run).
- Skipped: all Phase 1 "Tests that must pass" — deferred to the implementer once the terminal is back. No automated test suite is set up yet (this is a static site; Phase 1 verification is build + manual visual checks).

## 4. Decisions made and rationale (delta on the decision log)
- [docs/decisions/0001-framework-nextjs.md](../../decisions/0001-framework-nextjs.md) — Next.js 16 (App Router, SSG) over Astro/plain HTML.
- [docs/decisions/0002-content-data-files-reverse-render.md](../../decisions/0002-content-data-files-reverse-render.md) — commented TS data files; render reversed (newest first); auto-generated indices.
- [docs/decisions/0003-skills-certs-unified-lists.md](../../decisions/0003-skills-certs-unified-lists.md) — unify Skills & Certifications as matching indexed lists.
- [docs/decisions/0004-styling-global-css-variables.md](../../decisions/0004-styling-global-css-variables.md) — ported global CSS + CSS-variable theming (no Tailwind/shadcn).
- [docs/decisions/0005-hosting-vercel-auto-deploy.md](../../decisions/0005-hosting-vercel-auto-deploy.md) — Vercel auto-deploy from GitHub; build-gated.
- [docs/decisions/0006-fonts-nextfont-images-nextimage.md](../../decisions/0006-fonts-nextfont-images-nextimage.md) — `next/font` + `next/image`.
- [docs/decisions/0007-scope-trims-palettes-tweaks-grain.md](../../decisions/0007-scope-trims-palettes-tweaks-grain.md) — 2 palettes only; drop Tweaks panel; grain as config flag.

All 7 are `Status: accepted`, none superseded. (ADR-integrity audit script not run — terminal down; links were authored consistently.)

## 5. Outstanding questions for the user
- None block Phase 1 verification or Phases 2–4 implementation.
- Deferred (needed by Phase 5, not before): real image assets (portrait, 5 photography images, 2 project photos) + real `Nirmita-Dave-CV.pdf`; custom domain vs default `*.vercel.app`; film-grain default intensity; GitHub repo/remote + Vercel project ownership. See `docs/00_plan.md` → Deferred questions.

## 6. Known issues and tech debt added
- **Nothing has been executed** — treat all Phase 1 code as unverified until the build passes.
- **Cormorant Garamond font** configured as variable (no explicit weight) — may need `weight: ["400","500","600"]` added in `layout.tsx` if the build complains (medium likelihood).
- **Broken images expected** until assets are added: photo/portrait paths in the content files point at files not yet in `public/images/...`. Project tiles without an `img` fall back to placeholders automatically; photos/portrait do not.
- **No git branch/commit yet.** Per git-protocol this phase should live on `phase-1-scaffold`. Nothing committed (also per user rule: commit only when asked). When the implementer commits, create that branch first.
- **Sections are stubs.** `page.tsx` renders empty `#about/#photography/#work/#skills`; Hero/About are Phase 2, Photography Phase 3, Built Work + Skills/Certs Phase 4.
- `next-env.d.ts` is committed-as-written but is in `.gitignore` (Next regenerates it on build — expected).

## 7. Files most relevant to the next session
- `docs/01_scaffold-theme.md` — the phase contract: steps, tests-that-must-pass, acceptance criteria. Drive Phase 1 from here.
- `src/app/layout.tsx` — fonts + no-flash script (the Cormorant watch item lives here).
- `src/app/globals.css` — the entire design system / theme tokens.
- `src/components/{Nav,ThemeControls,Reveal,Footer}.tsx` — the shell to verify.
- `src/content/*.ts` — content contract for Phases 2–4; never hardcode copy/images in components.
- `design_handoff_portfolio/README.md` + `design-reference/{index.html,styles.css}` — pixel-level design source of truth.
- `docs/00_plan.md` — phase list, DoD, deferred questions.

## 8. Environment state
- Migrations run: none (no database).
- Env vars added: none (no secrets; site is fully static).
- Services started: none.
- Ports occupied: none yet (`next dev` will use **3000**).
- Installed deps: **none** — `node_modules` and a lockfile do not exist yet; `npm install` is required.
- Toolchain target: Node 24 LTS (`.nvmrc`), Node >= 20.9 required by `package.json`.

## 9. Exact command sequence to resume the dev environment
```bash
# From project root: C:\Users\jkhbu\Desktop\codex\nd-portfolio
npm install

# run the dev server (http://localhost:3000)
npm run dev

# if "Missing weight for font Cormorant_Garamond" appears:
#   edit src/app/layout.tsx → add  weight: ["400","500","600"]  to the
#   Cormorant_Garamond({...}) loader, then rerun npm run dev

# verify a clean production build + lint before closing Phase 1:
npm run build
npm run lint
```
